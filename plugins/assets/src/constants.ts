/** Broadcast whenever a file is added, removed, or changed under the managed directory. */
export const CHANGED_EVENT = 'devframes:plugin:assets:changed'

/** Preferred standalone CLI port. */
export const DEFAULT_PORT = 9015
